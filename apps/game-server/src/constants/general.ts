/**
 * The valid operations for upgrading or building.
 */
export const UpgradeOrBuildOperation = ["start", "end", "end-with-gem"];

// types
export type UpgradeOrBuildOperationType = "start" | "end" | "end-with-gem";

//
