# Use the global pnpm store instead of a project-local .pnpm-store/ directory.
store-dir=~/.local/share/pnpm/store

# Avoid reflink/hardlink failures on macOS and in restricted environments.
package-import-method=copy

block-exotic-subdeps=false
